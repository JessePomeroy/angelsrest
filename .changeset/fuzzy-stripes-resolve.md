---
"@jessepomeroy/crm-api": patch
---

Declare the Stripe runtime dependency required by the shipped Convex fee-capture action so clean downstream installations type-check and deploy without relying on a host dependency.
