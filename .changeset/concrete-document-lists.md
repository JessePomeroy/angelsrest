---
"@jessepomeroy/crm-api": patch
---

Replace the unused generic tenant-query dispatcher with concrete invoice, quote and contract list reads, preserving authorization, indexed status filtering and the 200-row cap.
