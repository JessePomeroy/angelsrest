---
"@jessepomeroy/crm-api": patch
---

Write client activity through a schema-typed internal helper instead of dispatching a nested mutation. Preserve callers' authorization, activity payloads and transaction behavior.
