---
"@jessepomeroy/crm-api": patch
"@jessepomeroy/print-catalog": patch
---

Add opt-in reservation-time print instructions with immutable artwork/finish identity and a distinct paid shipping recipient. Preserve existing reservation and order behavior until a host explicitly adopts the new input. Share the existing provider product-option mapping without changing its output.
