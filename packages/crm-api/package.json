{
	"name": "@jessepomeroy/crm-api",
	"version": "1.0.0",
	"description": "Generated Convex API types for the photographer CRM platform. Consumed by spoke sites (reflecting-pool, future clients).",
	"type": "module",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		},
		"./api": {
			"types": "./src/api.ts",
			"default": "./src/api.ts"
		},
		"./dataModel": {
			"types": "./src/dataModel.ts",
			"default": "./src/dataModel.ts"
		},
		"./server": {
			"types": "./src/server.ts",
			"default": "./src/server.ts"
		}
	},
	"files": [
		"src",
		"dist",
		"convex"
	],
	"scripts": {
		"build": "node -e \"console.warn('crm-api: real build wired in Gap 2 Phase 2 (tsc \\u2192 dist/). Phase 1 only relocated convex/ into the package; consumers still resolve via link: + Vite raw-.ts transpile.')\""
	},
	"peerDependencies": {
		"convex": "^1.30.0"
	},
	"devDependencies": {
		"convex": "^1.34.1",
		"typescript": "^5.9.3"
	},
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/jessepomeroy/angelsrest.git",
		"directory": "packages/crm-api"
	}
}
