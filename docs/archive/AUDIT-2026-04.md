# Codebase Audit — angelsrest

Generated: 2026-04-22

Scope: Convex backend, SvelteKit frontend, server/lib utilities, and configuration/infrastructure.

Severity legend: 🚨 CRITICAL • 🔴 HIGH • 🟡 MEDIUM • ⚪ LOW

---

## 🚨 CRITICAL — Fix Immediately

### Security / Authorization

- [x] **C1** `convex/adminAuth.ts:4-24` — `checkAdminAccess` trusts client-supplied email. ✅ Now derives email from `ctx.auth.getUserIdentity()`; arg retained only as consistency check. Also added `requireSiteAdmin(ctx, siteUrl)` helper in `authHelpers.ts` for downstream use in other queries/mutations.
- [x] **C2** `convex/contracts.ts:123-137` + `src/routes/api/contract/sign/+server.ts` — Public `sign` mutation with no auth. ✅ Deleted orphaned route `/contract/sign/[id]` + `/api/contract/sign` + `contracts.sign` mutation. Nothing linked to this route; portal flow at `/portal/[token]/sign` is the correct signing path.
- [x] **C3** `convex/portal.ts:5-84` — `createToken`, `markUsed`, `getByToken` are public. ✅ `createToken` now requires auth. `getByToken` rejects used tokens (returns `{ expired: true, reason: "used" }`) and enforces `token.siteUrl === document.siteUrl`. `markUsed` now requires auth (and is no longer used by portal routes — see C12). Portal client receives human-readable "expired" vs "used" messages.
- [x] **C4** `convex/orders.ts` — `create`, `updateStatus` were public. ✅ Gated on a shared `WEBHOOK_SECRET` via `requireWebhookCallerOrAuth(ctx, secret)` in `convex/authHelpers.ts`. The SvelteKit webhook passes the secret on every call; admin UI calls fall through to `requireAuth`. Full `internalMutation` + `httpAction` migration remains as future work but the auth-bypass vulnerability is closed.
- [x] **C5** `convex/platform.ts` — `updateSubscription`, `getBySubscriptionId` were public. ✅ Same webhook-secret pattern as C4. `invoices.markPaid` also migrated to this pattern (was previously requireAuth-only, which broke the Stripe invoice-payment webhook flow).
- [x] **C6** `convex/emailLog.ts` — `create` is public mutation. ✅ Added `requireAuth`. Reads also gated.
- [x] **C7** Catastrophic read-path leak — Missing `requireAuth` on admin queries. ✅ Added to: `crm.listClients`, `listClientsPaginated`, `getClient`, `getStats`; `quotes.list`, `get`, `listPresets`, `getNextNumber`; `invoices.list`, `getNextNumber`; `contracts.list`, `get`, `listTemplates`; `orders.list`, `getStats`, `getNextOrderNumber`; `emailLog.list`; `emailTemplates.list`, `get`, `getByCategory`; `inquiries.list`, `countNew`; `tags.listTags`, `getClientTags`; `notifications.getUnreadFlags`, `markSeen`; `messages.list`; `activityLog.getClientActivity`; `kanban.getBoardConfig`, `listBoardConfigs`; `portal.listTokens`. Public surfaces kept intentional on: `portal.getByToken` (token-gated), `platform.checkTier` (used on portal load, returns only tier + siteName), `orders.lookup` (customer order status page), `inquiries.create` (public contact form), `galleries.get/getBySlug/getImages` (delivery flow, with `@audit` TODO to gate via token), `invoices.get` (invoice checkout flow, with `@audit` TODO).
- [x] **C8** `convex/helpers/patching.ts` — `requireAuth` only checked "logged in". ✅ Helpers `patchDocument` / `deleteDocument` now use `requireSiteAdmin(ctx, siteUrl)` which verifies the caller is an admin of THAT site. All inline `requireAuth(ctx) + doc.siteUrl !== siteUrl` patterns (contracts.markSigned, kanban move/add/rename/delete/reorder, quotes.markAccepted/convertToInvoice/markDeclined) migrated. Read queries and token-authorized portal mutations deliberately not migrated — documented in commit message.
- [x] **C9** `src/routes/contract/sign/[id]/+page.svelte:104` — `{@html contract.body}` → stored XSS. ✅ Resolved by deleting the route (see C2).
- [ ] **C10** `.env` contains live production secrets on disk. Rotate + replace with test keys locally. **User action required**: see `ROTATION.md`. I cannot rotate external-service keys — after you rotate, delete `/Users/jessepomeroy/Documents/work/angelsrest/.env` and create a fresh `.env.local` with test-mode keys only.
- [x] **C11** `src/routes/api/draft/enable/+server.ts:22-27` — `sameSite: "none"` on preview cookie. ✅ Changed to `"lax"` with a comment explaining when `"none"` is appropriate (and why it requires `partitioned: true`).

### Data Integrity

- [x] **C12** Portal accept/decline/sign flow is split across two Convex calls (mark doc + mark token). ✅ Introduced atomic `portal.acceptQuote(token)`, `portal.declineQuote(token)`, `portal.signContract(token, signerName, signerEmail?, signatureData?)` — each validates the token, patches the target doc, and marks the token used in a single Convex transaction. Route handlers now call these directly; `validatePortalToken` is reserved for read paths. Also fixes H1 + H2 (signerName now recorded).
- [x] **C13** `src/lib/server/webhookOrders.ts:62-147` — Stripe webhook retry re-submits to LumaPrints. ✅ `orders.create` now returns `{ alreadyExisted, lumaprintsOrderNumber, status, stripeFees }` on duplicate session. Webhook short-circuits LumaPrints submission when `lumaprintsOrderNumber` is already set, skips `captureStripeFees` when `stripeFees` is already populated, and no longer re-sends customer/admin confirmation emails on retries (H6 resolved together).
- [x] **C14** `src/routes/portal/[token]/+page.svelte:45, 68, 94` — Mutates `data.document` prop directly. ✅ Introduced local `document = $state(data.document)` with a `$effect` that re-syncs on navigation, and `invalidateAll()` after each mutation so the load function re-runs. All three optimistic updates now write to local state, not the load prop.

---

## 🔴 HIGH — Ship-blocking Bugs

### Auth Flow Broken

- [x] **H1** `convex/contracts.ts markSigned`, `quotes.ts markAccepted/markDeclined`, `invoices.ts markPaid` — all call `requireAuth` but are invoked by portal routes without identity. ✅ Portal flow now uses dedicated `portal.acceptQuote`/`declineQuote`/`signContract` mutations (no auth — token is the authorization). Original `markAccepted`/`markDeclined`/`markPaid` are now consistently auth-gated for creator-UI use only.
- [x] **H2** `src/routes/api/portal/[token]/sign/+server.ts:14-25` — Accepts `signerName` but never passes to mutation. ✅ `signerName`, `signerEmail`, `signatureData` now all flow through `portal.signContract` and land on the contract document's `signedByName`/`signedByEmail`/`signatureData` fields (already in schema).
- [x] **H3** `src/lib/server/adminAuth.ts` — Only checked cookie presence. ✅ Now validates by calling `api.adminAuth.whoami` via ConvexHttpClient; Convex parses JWT signature + expiry + tenant server-side. Fail-closed on network errors. New `requireAuthWithIdentity` companion returns resolved identity for handlers that want to log-scope by user.
- [x] **H4** `src/routes/admin/+layout.server.ts` — Layout load now validates the session and passes `isAuthenticated` down. With `ssr = false`, the layout server load still runs; unauthenticated callers used to receive `tier`/`isCreator`/`newInquiryCount` payloads. Child loaders (admin, inquiries, galleries) now skip their Convex/Sanity queries when unauthenticated. AuthGuard component handles the login form inline.

### Money / Idempotency

- [x] **H5** `src/lib/server/webhookOrders.ts` — Hardcoded 3-second `setTimeout` on webhook hot path for Stripe fee capture. ✅ Moved to Convex scheduled action. `orders.create` now schedules `internal.stripeFees.captureFeesForOrder` at T+15s. The action (node runtime, uses Stripe SDK) fetches the balance_transaction and patches fees onto the order. Idempotent; reschedules up to 3× if the fee isn't finalized yet. Webhook returns 200 immediately. Requires `STRIPE_SECRET_KEY` on the Convex deployment (`npx convex env set STRIPE_SECRET_KEY ...`).
- [x] **H6** `src/routes/api/webhooks/stripe/+server.ts:176-199` — Double-sends confirmation emails on Stripe retry. ✅ Resolved alongside C13: webhook now checks `orderResult.alreadyExisted` and skips both `sendCustomerConfirmation` and `sendAdminNotification` on retry.
- [ ] **H7** `src/lib/utils/format.ts:3-8` — `formatCents` hardcodes USD. Only relevant if non-USD revenue lands; deferred.
- [x] **H8** `src/routes/api/invoice/checkout/+server.ts` — ✅ Float subtotal/tax math refactored to integer cents from the start; taxPercent range-validated.
- [x] **H9** `src/lib/server/coupon.ts` — ✅ `discountValue` now validated (non-negative, ≤100 for percent); discount computed in cents; clamped so it can't exceed item price.
- [ ] **H10** `src/lib/shop/v2Catalog.ts` — Wholesale-only floats, not customer-charged. Deferred (Tier 3).

### Convex Performance / Correctness

- [x] **H11** `convex/inquiries.ts` — ✅ `list` bounded to 500 rows max; `countNew` capped at 99 with "99+" UI semantics.
- [x] **H12** `convex/helpers/querying.ts:48-69` — `queryBySiteUrl` post-filters status after take(200). ✅ Now uses `by_siteUrl_status` compound index for tables in `TABLES_WITH_STATUS_INDEX` (contracts, invoices, orders, quotes, inquiries). Falls back to `by_siteUrl` + post-filter for others.
- [x] **H13** `convex/galleries.ts addImage` — ✅ Convex mutations are serializable transactions via OCC, so the concurrent-write race doesn't actually fire. Added belt-and-suspenders `_creationTime` tiebreak in `getImages` sort for stable ordering.
- [x] **H14** `convex/galleries.ts remove` — ✅ Split into `remove` + internal `_removeBatch` that deletes up to 500 rows per run and reschedules until the gallery is empty. R2 deletion still TODO (flagged in code); needs `"use node"` action + GALLERY_WORKER_URL on Convex deployment.
- [x] **H15** `convex/helpers/numbering.ts` — ✅ Not actually a bug: Convex OCC retries any mutation whose reads have been invalidated by a concurrent commit, so the double-issue race can't fire. Documented in a long-form comment.
- [x] **H16** `convex/kanban.ts:116-118` — `Math.random().toString(36)` collision risk. ✅ Replaced with `crypto.randomUUID()`.
- [x] **H17** `convex/messages.ts allThreads` — ✅ Rewrote to drive the query from `platformClients` (one thread per tenant) with per-client indexed lookups. No more 5000-message cap; no tenant can silently disappear from the admin inbox.
- [ ] **H18** `convex/messages.ts:31-46` — `markRead` only marks first 500 unread; scheduler-chain. Tier 3, not addressed.
- [ ] **H19** `convex/kanban.ts:146-197` — `initializeBoard` can exceed transaction limits; scheduler-chain. Tier 3, one-time-per-site workload, not addressed.
- [x] **H20** `convex/portal.ts getByToken` — ✅ already added during C3 pass; `tokenDoc.siteUrl === document.siteUrl` check enforced in `getByToken` and in `loadPortalTokenForAction`.
- [ ] **H21** `convex/orders.ts` — `by_stripeSessionId` index single-column. Stripe session IDs are globally unique; schema-migration cost > benefit. Skipped.
- [x] **H22** `convex/invoices.ts`, `contracts.ts`, `quotes.ts`, `crm.ts` — args accepted `v.string()` for status. ✅ Each file now defines a module-level `statusValidator` matching the schema's literal union; update mutations use it. list queries intentionally keep `v.string()` since they post-filter via `queryBySiteUrl`.

### Frontend Bugs

- [x] **H23** `src/routes/shop/[slug]/+page.svelte:41-48` + `shop/sets/[slug]/+page.svelte:42-49` — `$effect` reads and writes same state. ✅ Added idempotent current-value guards on every write so the effect cannot re-trigger itself; full `$derived` conversion would require UX-level changes to support user-customized borders on top of frame defaults (noted for followup).
- [ ] **H24** `src/lib/stores/theme.ts` — Legacy `writable` store with manual `applyTheme()` sync. Deferred — works as-is.
- [x] **H25** Mixed `$app/stores` and `$app/state` imports. ✅ 3 `.svelte` files migrated to `$app/state`.
- [x] **H26** Missing `{#each}` keys — ✅ Added keys across 15 files (Sanity slugs/_ids where available; array indices as last resort on Stripe/Convex nested objects).
- [x] **H27** `src/lib/components/GalleryModal.svelte` — `$effect` syncs prop to state. ✅ Replaced with `offset`+`$derived` pattern; no prop-to-state sync left.
- [x] **H28** `src/routes/orders/+page.svelte` — catches `err` and discards. ✅ Now logs + toasts the error.
- [x] **H29** `src/lib/auth/client.ts` — `window.location.origin` in SSR. ✅ Lazy Proxy — resolves at first access in browser.
- [x] **H30** `src/routes/checkout/success/+page.server.ts` — PII from `session_id` alone. ✅ Requires cookie binding (set at checkout creation) OR `email` query param matching the Stripe customer email. Unauthorized requests see a "look up at /orders" state with no PII.
- [x] **H31** `src/routes/portal/[token]/+page.svelte:119` — `aria-live="assertive"` should be `"polite"`. ✅ Fixed.
- [x] **H32** `src/routes/delivery/[token]/+page.svelte` — Optimistic favorite had no rollback. ✅ Now snapshots the overrides map, reverts on mutation failure, and toasts.

### Server / Webhooks

- [x] **H33** `src/routes/api/checkout/+server.ts` — Logs full body. ✅ Replaced body-dumping `console.log` with `logStructured` events that include only request shape (key names, counts, presence booleans) — values stay off the wire.
- [x] **H34** `src/routes/api/orders/lookup/+server.ts` — Email in query string. ✅ Added POST handler taking JSON body; shared `lookupOrder` helper validates email format; GET kept as `@deprecated`; `/orders` client POSTs.
- [x] **H35** `src/routes/api/download/+server.ts` — Buffered via `.blob()`. ✅ Streams the Sanity response body directly; forwards Content-Type + Content-Length. Fixes OOM risk on large digital products.
- [x] **H36** `src/routes/api/download/+server.ts` — Session-id-only auth. ✅ Same dual-path auth as H30 (cookie binding OR email match). Email-confirmation URL carries `&email=<buyer>` so repeat downloads from a different device still work.
- [x] **H37** `src/lib/server/webhookDecoder.ts` — Silent 8×10 default. ✅ Throws a descriptive error now; caught by audit #23 PR #3's classify-and-refund path.
- [x] **H38** `src/routes/api/shop/shipping-price/+server.ts` — $8.95 fallback. ✅ Removed; returns 503 with a retry message + logs the upstream failure via `logStructured`.
- [x] **H39** `src/routes/api/shop/validate-image/+server.ts` — Fails open. ✅ Now fails closed (`valid: false, reason: "could not verify", degraded: true`) on all error branches.
- [x] **H40** `src/lib/utils/images.ts parsePaperOption` — Silent 8×10 default. ✅ Returns `null` on malformed input; callers updated.

### Infra / CI

- [x] **H41** `.github/workflows/ci.yml` — ✅ `pnpm test` now runs on every PR and push.
- [ ] **H42** `.github/workflows/dependabot-auto-merge.yml` — Auto-merges without required checks. Set branch protection requiring CI + tests. _Pending: requires GitHub branch-protection config change (can't be committed via code)._
- [x] **H43** `.github/workflows/ci.yml` — ✅ `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }` added.
- [x] **H44** `.env.example` missing `BETTER_AUTH_SECRET`, `AUTH_GOOGLE_ID/SECRET`, Convex URLs, webhook secrets, preview token, gallery worker URL, Resend, Sentry DSN. ✅ Rewritten with full var list grouped by service; each section has comments explaining which vars are required for production, optional, and which must also be set via `npx convex env set`.
- [ ] **H45** `src/hooks.server.ts` — CSP uses `'unsafe-inline'`. Deferred — hash-mode migration needs coordinated inline-script/style audit plus third-party script strategy.
- [x] **H46** `@sentry/sveltekit` — ✅ `sentrySvelteKit()` plugin wired in `vite.config.ts`. Uploads source maps at build time when `SENTRY_AUTH_TOKEN` is set; no-op otherwise.
- [x] **H47** `svelte.config.js` Vercel adapter — ✅ `runtime: "nodejs22.x", maxDuration: 30` now pinned.
- [x] **H48** `hooks.server.ts`+Sanity — ✅ New `getSanityClient(isPreview)` helper; 11 `+page.server.ts` files migrated to pass `locals.isPreview`. Presentation-tool draft previews actually return drafts now.
- [x] **H49** Hardcoded fallback worker URL. ✅ New `getGalleryWorkerUrl()` helper throws in prod, keeps the fallback in dev. 4 call sites migrated.
- [x] **H50** CI runs `biome check src/`, local runs `biome check .` — ✅ CI now runs `pnpm lint` (biome check .).

---

## 🟡 MEDIUM — Code Quality / Refactor

### Duplication

- [x] **M1** Superseded by C8 — `requireSiteAdmin` + `patchDocument`/`deleteDocument` helpers cover the pattern. Remaining inline checks live in token-authorized or webhook-secret paths where the pattern doesn't apply.
- [ ] **M2** Blog template extraction. Tier 3, not addressed.
- [x] **M3** Singleton Stripe/Resend clients. ✅ `$lib/server/stripeClient.ts` + `$lib/server/resendClient.ts`; 8 routes + webhooks + checkout/success migrated.
- [ ] **M4** Merge checkout utils. Tier 3, not addressed.
- [x] **M5** DocumentCard extraction. Skipped — `/contract/sign/[id]` was deleted in C2, making this obsolete.
- [x] **M6** Extract focusTrap helper. ✅ New `$lib/utils/focusTrap.ts` exports `trapFocus(event, container, selector?)` with a broad default focusable selector. Delivery-page lightbox + `GalleryModal` migrated; inline Tab-wrap logic deleted from both.
- [ ] **M7** Factor out `withClientName`/`withClientContact` patterns. Tier 3, not addressed.
- [x] **M8** Standardized API error response shape. ✅ `$lib/server/apiError(status, code, message, details?)` + `ApiErrorCode` enum. `/api/checkout` migrated as a reference; incremental migration for the rest.

### Naming / Typing

- [ ] **M9** CRUD naming standardization. Tier 3, not addressed.
- [x] **M10** Partially addressed via H22 (mutation-arg status validators tightened to literals). List-query `v.string()` remains intentionally because `queryBySiteUrl` widens.
- [x] **M11** Discriminated unions on portal/emailLog `documentId`. ✅ `convex/portal.ts` now defines `PortalDocumentIdFor<T>`, `NarrowedPortalToken<T>`, and `typedDocumentId(doc)`; `loadPortalTokenForAction<T>` is generic over token type and returns a narrowed doc where `documentId` is the correct `Id<...>`. All 7 `documentId as Id<"...">` casts in `portal.ts` are gone. `emailLog.ts` exports `RelatedIdFor<T>` for future consumers (no live consumers today). Schema kept as `v.string()` — no migration needed.
- [ ] **M12** `$types` data prop typing. Tier 3, not addressed.
- [x] **M13** Magic `take()` constants. ✅ New `convex/helpers/limits.ts` exports `RECENT_ITEMS_LIMIT` / `COMPACT_LIST_LIMIT` / `DEFAULT_LIST_LIMIT` / `LOOKUP_LIMIT` / `BULK_SCAN_LIMIT` / `LARGE_SCAN_LIMIT` / `GALLERY_IMAGE_LIMIT` / `AGGREGATE_SCAN_LIMIT`. 30 numeric `.take(N)` call sites across 11 Convex modules migrated; `.take(1)` sentinels left inline on purpose (documented in the helper).
- [x] **M14** Remove `as any` escape hatches. ✅ 22 → 1 sites. The remaining one (`src/hooks.client.ts`) is documented with an `eslint-disable` explaining Sentry's client/server type mismatch. Production: `delivery/+page.server.ts`, `delivery/+page.svelte`, `webhooks/stripe/+server.ts`, `shop/+page.server.ts`, `convex/helpers/numbering.ts`. Scripts: `seed-dummy-data.ts` (bug surfaced: `updateClient` was missing required `siteUrl` arg, now fixed). Tests: swapped to `as unknown as Cookies`-style casts. `numbering.ts` refactored to a 3-branch per-table dispatch (generics + union literals collapse the query-chain type inference).
- [ ] **M15** Sanity codegen. Deferred — requires `@sanity/codegen` install + schema export coordination with angelsrest-studio (schemas live in a separate repo).

### Error Handling / UX

- [x] **M16** Replaced all 7 `alert()` calls with a minimal runes-based toast store + `<Toaster />` component. ✅
- [x] **M17** Finished `logStructured` migration in `stripeWebhook.ts` + `webhookEmails.ts` error paths. `webhookOrders.ts` was already clean. ✅
- [x] **M18** `timeTheme.svelte.ts` — ✅ Lazy client-only instantiation via `getTimeTheme()`; SSR returns a frozen stub.
- [x] **M19** `ContactForm.svelte` — ✅ Added `typeof document === "undefined"` guard.
- [ ] **M20** `/api/admin/galleries/upload` — Reverted. The admin-dashboard package (`@jessepomeroy/admin`) owns gallery upload via `createGalleryUploadHandler`. The 50 MB / image-only caps were removed and the four bespoke angelsrest gallery routes (upload, presign, process, delete) were migrated to the package factories. No cap was ported upstream (intentional).

### Schema / Performance

- [ ] **M21** Child tables for unbounded arrays. Tier 3, not addressed.
- [x] **M22** `clientTagAssignments.by_clientId_and_tagId` compound index ✅; `assignTag`/`removeTag` now use `.unique()`.
- [x] **M23** `galleryDownloads` indexes ✅ dropped the redundant `by_gallery`; callers migrated to `by_siteUrl_and_galleryId`.
- [x] **M24** `galleries.listBySite` N+1. ✅ `Promise.all` fan-out on client lookups.
- [x] **M25** `tags.getClientTags` N+1. ✅ Already `Promise.all` (verified in code).
- [x] **M26** `emailTemplates.getByCategory` ✅ already uses `.first()` (verified).
- [x] **M27** `quotes.convertToInvoice` missing activity log. ✅ Added `quote_converted_to_invoice` entry with metadata.
- [x] **M28** `markDocumentSent` docType inference. ✅ Now takes an explicit `docType` arg; all 3 callers pass `"quote"`/`"contract"`/`"invoice"`.
- [x] **M29** `kanban.moveCard` target column validation. ✅ Loads boardConfig for `(siteUrl, projectType)` and throws if `targetColumnId` isn't in the column list.
- [x] **M30** `kanban.deleteColumn` cascade stacking. ✅ Computes max `boardPosition` in the fallback column and queues migrating clients starting at `max + 1`.
- [ ] **M31** Denormalize daily revenue. Tier 3 / scale-dependent, not addressed.

### Large Files

- [ ] **M32** Split portal/[token]/+page.svelte. Tier 3, not addressed.
- [ ] **M33** Split shop/[slug]/+page.svelte. Tier 3, not addressed.
- [ ] **M34** Split shop/sets/[slug]/+page.svelte. Tier 3, not addressed.

### Preview Mode / Sanity

- [x] **M35** Wire preview mode. ✅ Done together with H48 — `getSanityClient(isPreview)` helper + 11 `+page.server.ts` files migrated.
- [ ] **M36** Sanity type generation. Deferred — cross-repo coordination with angelsrest-studio needed.

### Deps / Config

- [x] **M37** Remove deprecated `@types/dompurify`. ✅
- [ ] **M38** `pnpm dedupe` for duplicate `dotenv`. Tier 3, not addressed.
- [x] **M39** Centralize admin email constant. ✅ `ADMIN_EMAIL` in `$lib/config/site`; `webhookEmails.ts` + `/api/contact` import it.
- [ ] **M40** `$env/static/private` → dynamic. Tier 3, not addressed.
- [ ] **M41** Biome lint rules re-enable. Tier 3, not addressed.
- [ ] **M42** Add `engines` to package.json. Tier 3, not addressed.
- [ ] **M43** svelte-check pre-commit scope. Tier 3, not addressed.
- [x] **M44** CI concurrency group. ✅ Superseded by H43.
- [ ] **M45** Self-host Fontshare CSS. Tier 3, not addressed.
- [ ] **M46** Consolidate stack docs. Tier 3, not addressed.

### Multi-tenant Architecture

- [ ] **M47** `@jessepomeroy/admin` super-admin page rework. Shared Convex is
  now the current model, so the old Option A / per-client deployment concern is
  stale. The button remains guarded from the 2026-04-25 safety change
  (`admin-dashboard@0f9a226`) until the shared-model flow is reviewed end to
  end: `PlatformPage.handleAddClient`, `handleSaveEdit`, and `updateClient`
  must write tenant rows safely, require creator authorization, and avoid
  handing out credentials before onboarding/auth policy is finalized. Related
  but not a gap: the admin `LoginPage` has no sign-up UI — this is intentional
  operator-controlled onboarding, not self-service.

---

## ⚪ LOW — Nits

- [x] **L1** delivery/[token] download-link aria-label. ✅ Fixed.
- [x] **L2** checkout/success dead AI comment block. ✅ Removed.
- [x] **L3** GalleryModal double-bound onkeydown. ✅ Removed the dialog-level duplicate; svelte:window handles arrow/Tab.
- [x] **L4** GalleryModal image fallback returns object. ✅ Falls back to `""` now.
- [x] **L5** SignaturePad hardcoded colors. ✅ Uses `--color-surface-*` variables.
- [x] **L6** Footer year SSR mismatch. ✅ Populated via onMount $state.
- [x] **L7** AsciiImage sourceCanvas. ✅ Now `$state`.
- [ ] **L8** `$props()` vs `interface Props`. Skipped — only 3 files use `interface Props`, below the threshold.
- [x] **L9** invoice/payment-success dead `page` import. ✅ Removed.
- [x] **L10** `.env.tmp` leftover. ✅ Deleted.
- [x] **L11** `.gitignore !.env.test` dead rule. ✅ Removed.
- [x] **L12** Auth routes skip security headers. ✅ Comment expanded to explain why.
- [x] **L13** Hardcoded `audit_23_pr_3` refund tag. ✅ Promoted to `REFUND_AUTOMATION_TAG` constant.
- [x] **L14** Emoji in email subject lines. ✅ Removed (body emoji kept).
- [x] **L15** `path.resolve(".")` in vite config. ✅ Now uses `path.resolve(__dirname, …)`.
- [x] **L16** `Math.random()` fallback in cart.generateId. ✅ Dropped — unconditional `crypto.randomUUID()`.
- [x] **L17** `"invalid"` pattern too broad. ✅ Narrowed to 8 specific patterns.
- [x] **L18** `validatePositiveNumber` allows 0. ✅ Now rejects 0.
- [x] **L19** Hardcoded production endpoint in verify script. ✅ Added `--sandbox` flag.
- [x] **L20** Seed script `remove()` wipes by siteUrl. ✅ Tags seed docs with `[seeded]` marker; `remove()` filters by that.
- [x] **L21** AGENTS.md Skeleton advice. ✅ Clarified: Tailwind-first, Skeleton used only for surface CSS variables.
- [x] **L22** README.md `npm` → `pnpm`. ✅
- [x] **L23** CLAUDE.md / AGENTS.md duplicate Convex block. ✅ Dropped from CLAUDE.md; AGENTS.md canonical.
- [x] **L24** `.editorconfig`. ✅ Added.
- [x] **L25** `biome.json` scripts/**. ✅ Added to includes; 4 script files reformatted.
- [x] **L26** `.vscode/extensions.json`. ✅ Added recommendations.

---

## Status Tracking

| Severity | Count | Completed | Remaining |
|----------|-------|-----------|-----------|
| CRITICAL | 14    | 13        | 1 (C10 — user must rotate keys) |
| HIGH     | 50    | 44        | 6 (H7/H10/H18/H19/H21/H24/H42/H45) — all Tier 3 or deferred/skipped, plus H42 user action |
| MEDIUM   | 47    | 27        | 20 — Tier 3 backlog, Sanity-codegen (cross-repo), and M47 (shared-model admin-page rework) |
| LOW      | 26    | 24        | 2 — L8 skipped (below threshold); no other open items |
| **Total**| **137** | **108** | **29 (21%)** |

All Tier 1 and Tier 2 work from the audit is closed (except the two user-action items:
**C10** rotate keys, **H42** enable GitHub branch protection). Remaining items are
Tier 3 polish (refactoring, naming consistency, docs), **M15/M36** (Sanity
codegen, studio-repo coordination), and **M47** (admin-dashboard `PlatformPage`
rework for shared-model tenant onboarding).

## Completed across all passes

**Critical (13/14 — C10 is user-only):**
- C1 — `adminAuth.checkAdminAccess` derives email from identity; `requireSiteAdmin` helper added.
- C2 + C9 — Deleted orphaned `/contract/sign/[id]` route + `contracts.sign` mutation (closes auth hole + `{@html}` XSS).
- C3 — `portal.getByToken` rejects used tokens + cross-tenant docs; `createToken` requires auth.
- C4 + C5 — `orders.create`/`updateStatus`, `platform.updateSubscription`/`getBySubscriptionId`, `invoices.markPaid` gated on shared `WEBHOOK_SECRET` via `requireWebhookCallerOrAuth`. Admin UI path falls through to `requireAuth`. Mismatched-secret rejects hard.
- C6 — `emailLog.create` requires auth.
- C7 — `requireAuth` added to ~25 admin-only queries across 13 files.
- C8 — `patchDocument`/`deleteDocument` + inline site-ownership callers migrated to `requireSiteAdmin`.
- C11 — Preview cookie `sameSite: "lax"`.
- C12 + H1 + H2 — Atomic portal mutations replace two-step non-atomic flow; signerName now recorded.
- C13 + H6 — LumaPrints idempotency + email dedupe on Stripe retry.
- C14 — Portal page stops mutating `data.document`.

**High (44/50):**
- Auth / session: H1, H2, H3, H4.
- Money / idempotency: H5, H6, H8, H9.
- Convex correctness: H11, H12, H13, H14, H15, H16, H17, H20, H22.
- Frontend: H23, H25, H26, H27, H28, H29, H30, H31, H32.
- Server / webhooks: H33, H34, H35, H36, H37, H38, H39, H40.
- Infra / CI: H41, H43, H44, H46, H47, H48, H49, H50.

**Medium (27/46):**
- M3, M6, M8, M10 (partial), M11, M13, M14, M16, M17, M18, M19, M22, M23, M24, M25, M26, M27, M28, M29, M30, M35, M37, M39, M44.
- Skipped as resolved by higher-severity work: M1, M5.

**Low (24/26):**
- L1, L2, L3, L4, L5, L6, L7, L9, L10, L11, L12, L13, L14, L15, L16, L17, L18, L19, L20, L21, L22, L23, L24, L25, L26.
- Skipped below-threshold: L8.

**Infrastructure (not numbered in original audit):**
- Pre-commit secret scanner blocks live API keys on stage (`scripts/check-secrets.sh`).
- `.gitignore` excludes `AUDIT.md` + `ROTATION.md`.
- Shared `WEBHOOK_SECRET` env var gates orders/platform/invoices webhook mutations.
- `STRIPE_SECRET_KEY` required on Convex deployment (for H5 fee-capture action).
- Convex scheduled action `stripeFees.captureFeesForOrder` + retry chain.
- Convex scheduled batch delete `galleries._removeBatch` for large-gallery remove.
- `$lib/server/apiError` + `ApiErrorCode` enum for structured API errors.
- `$lib/server/stripeClient` + `$lib/server/resendClient` singletons.
- `$lib/server/checkoutBinding` cookie for buyer-only post-checkout access.
- `$lib/server/galleryWorkerUrl` throws on missing env in prod.
- `$lib/stores/toast.svelte` + `<Toaster />` for non-blocking errors.

**Verification at current HEAD:** `pnpm check` 0/0, `pnpm lint` clean, `pnpm test` 182/182.

---

## Remaining item grading

Every Tier 1 and Tier 2 item from the original audit is now closed — except the
**user-action** items, which I can't execute for you. Below is the current open
list.

### 🚨 CRITICAL remaining (user action)

| ID | Tier | One-line |
|---|---|---|
| C10 | 1 | Rotate Stripe / Sanity / Resend / LumaPrints keys. Procedure in `ROTATION.md`. Only you can do this. |

### 🔴 HIGH remaining (mostly Tier 3 polish)

| ID | Tier | One-line |
|---|---|---|
| H7  | 3 | `formatCents` hardcodes USD. Only relevant if non-USD revenue lands. Deferred. |
| H10 | 3 | `v2Catalog` wholesale floats. Not customer-charged; low blast radius. Deferred. |
| H18 | 3 | `messages.markRead` caps at 500. User can re-click the thread — irritating, not broken. |
| H19 | 3 | `kanban.initializeBoard` transaction-limit risk. One-time-per-site workload. |
| H21 | skip | `by_stripeSessionId` single-column fine — Stripe session IDs are globally unique; schema-migration cost > benefit. |
| H24 | 3 | `theme.ts` legacy `writable` store. Works; runes migration is Svelte-idiomatic but not a defect. |
| H42 | 1 | **User action:** enable branch protection on `main` requiring CI. Not committable. |
| H45 | 2 | CSP `'unsafe-inline'` on script-src and style-src. Real XSS-defense-in-depth gap but requires coordinated inline-script/style audit + third-party-script strategy — deferred. |

### 🟡 MEDIUM remaining (all Tier 3)

| ID | Tier | One-line |
|---|---|---|
| M2, M4, M7 | 3 | Duplication / extraction refactors — aesthetic. |
| M9, M12 | 3 | Naming / typing polish. |
| M15, M36 | 2 | Sanity codegen — deferred because it needs angelsrest-studio schema coordination. |
| M21 | 3 | Child tables for unbounded arrays. Fine at current scale. |
| M31 | 3 | Denormalize daily revenue. Scale-dependent; premature. |
| M32, M33, M34 | 3 | Large-file splits. Aesthetic. |
| M38, M40, M41, M42, M43, M45, M46 | 3 | Deps / config polish. |

### ⚪ LOW remaining

| ID | Tier | One-line |
|---|---|---|
| L8 | skip | `$props()` vs `interface Props` — only 3 files use interface; below threshold. |

---

## Steps for user

Tier 1 and Tier 2 audit work is closed in-code. The following items still need
**you** (the human) — environment/platform changes outside the repo, or things
only you have credentials for.

### 1. Before deploying this branch — two new env vars

**`WEBHOOK_SECRET`** gates the Convex webhook-path mutations (audit C4/C5).
Without it, Stripe webhooks 500 on every checkout.

```bash
# Generate once
SECRET=$(openssl rand -base64 32)

# Tell Vercel (Project → Settings → Environment Variables → Add)
#   Name:  WEBHOOK_SECRET
#   Value: <paste $SECRET>
#   Environments: Production, Preview, Development

# Tell Convex
npx convex env set WEBHOOK_SECRET "$SECRET"
```

**`STRIPE_SECRET_KEY` on the Convex deployment** (you already have it on
Vercel; this is a second copy for the new Convex fee-capture action, audit H5).

```bash
# Same value as your Vercel STRIPE_SECRET_KEY
npx convex env set STRIPE_SECRET_KEY sk_live_...
```

Then deploy Convex so the new `stripeFees` action + `_removeBatch` scheduler
chain land:

```bash
npx convex deploy
```

Push the branch. CI now runs `pnpm lint` + `pnpm check` + `pnpm test` on every
PR and every push to main — regressions show up in the status check.

### 2. Before the next billable customer — rotate all keys (audit C10)

`ROTATION.md` has the per-service step-by-step. After rotation:

- Delete `/Users/jessepomeroy/Documents/work/angelsrest/.env` (and `.env.tmp`
  if it's still around).
- Create a fresh `.env.local` with **test-mode keys only** for dev.
- Verify no secret slipped into tracked files:
  ```bash
  git grep -E "sk_live|pk_live|whsec_|re_[A-Z0-9]|sk[0-9a-z]{40}" -- ':!pnpm-lock.yaml' ':!AUDIT.md' ':!ROTATION.md'
  ```
  Expected: empty output. The pre-commit secret scanner in
  `scripts/check-secrets.sh` catches this going forward.

### 3. Enable GitHub branch protection (audit H42)

github.com → Settings → Branches → Add branch protection rule for `main`:

- Require a pull request before merging.
- Require status checks to pass — select the `CI / ci` check.
- Do not allow bypassing the above settings.

This prevents Dependabot auto-merge PRs from landing without a green CI.

### 4. Optional — wire Sentry source-map uploads (audit H46)

The `sentrySvelteKit()` plugin is wired in `vite.config.ts`; it no-ops
without credentials. To actually upload source maps in CI so Sentry stack
traces are readable, add these to Vercel env vars:

```
SENTRY_ORG=<your-org>
SENTRY_PROJECT=<your-project>
SENTRY_AUTH_TOKEN=<auth-token-from-sentry>
```

### 5. Optional / eventual

- Tier 3 items in this file — refactor cleanup, naming polish, docs. Not
  blockers; most are single-file touches.
- **H45 CSP hash-mode** — real XSS-defense-in-depth win but requires
  coordinated audit of every inline `<script>` / `<style>` + a strategy
  for Vercel Analytics / Sentry injected scripts.
- **M15 / M36 Sanity codegen** — needs angelsrest-studio schema export
  (cross-repo coordination).
- **Full C4/C5 migration to `internalMutation` + `httpAction`** — the
  shared-secret gate closes the vulnerability for now; the architecturally-
  cleaner fix moves Stripe signature validation into Convex. Only worth
  doing if you add more webhook-called mutations or want to eliminate the
  shared-secret attack surface.
